import { ProductCard } from "./components/ProductCard";
import "./styles.css";

export default function App() {
  return (
    <div className="App">
      <ProductCard onAddToCard={() => {}} />
      <ProductCard onAddToCard={() => {}} />
      <ProductCard onAddToCard={() => {}} />
      <ProductCard onAddToCard={() => {}} />
      <ProductCard onAddToCard={() => {}} />
      <ProductCard onAddToCard={() => {}} />
    </div>
  );
}
